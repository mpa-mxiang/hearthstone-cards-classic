import React from 'react';
import CardData from '../components/CardData';

const Data = ({ match }) => {
  return (
    <div>
      <CardData match={match} />
    </div>
  );
};

export default Data;
