import React from 'react';
import MetricData from '../components/MetricData';

const Data = ({ match }) => {
  return (
    <div>
      <MetricDetails match={match} />
    </div>
  );
};

export default Data;
